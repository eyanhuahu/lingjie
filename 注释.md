# 普通维护流程：只改 content.xlsx，图片放 images/

注意：换行使用alt+enter
普通维护者只需要关注仓库一级目录里的两个东西：

- `content.xlsx`
- `images/`

`content.xlsx` 是内容表。普通维护者主要修改它。

`images/` 是图片文件夹。如果只引用已有图片，不需要动它；如果要新增图片，才把图片上传到 `images/`。

## 修改文字内容

1. 打开仓库一级目录的 `content.xlsx`。
2. 在 `items` 表新增、修改或隐藏词条。
3. 保存。
4. 上传覆盖 GitHub 仓库一级目录的 `content.xlsx`。

如果只改文字内容，只上传 `content.xlsx` 即可。

## 修改分类

1. 打开 `content.xlsx`。
2. 修改 `sections` 表。
3. 保存并上传覆盖 `content.xlsx`。

## 修改网站标题

1. 打开 `content.xlsx`。
2. 修改 `site` 表。
3. 保存并上传覆盖 `content.xlsx`。

## 图片字段怎么写

如果图片已经在 `images/` 里，例如：

```text
images/1234.png
```

Excel 的“图片”字段只写：

```text
1234.png
```

也可以在 `images` 表里维护图片库：

- 图片名：灵界设定图
- 文件名：1234.png
- 说明：灵界世界观配图

然后 `items` 表的“图片”字段可以写图片名。第一版仍推荐直接填写文件名，最直观、最少出错。

## 详情里怎么插图

不要在 Excel 单元格里直接插入图片对象。网页读取的是字段文本，不读取 Excel 内嵌图片对象。

在“详情”字段里单独写一行：

```text
[[图片:1234.png]]
```

如果需要图片说明：

```text
[[图片:1234.png|图片说明]]
```

也支持英文写法：

```text
[[image:1234.png]]
[[image:1234.png|caption text]]
```

## 新增图片时要上传什么

如果新增图片，需要上传两类文件：

- 修改后的 `content.xlsx`
- `images/` 下的新图片

图片上传位置必须是仓库一级目录的 `images/` 文件夹。

## 图片文件名要求

- 推荐英文小写、数字、短横线、下划线。
- 不建议中文、空格、括号、特殊符号。
- 文件名大小写必须和 Excel 中完全一致。
- `.jpg` 和 `.png` 是不同文件。

## GitHub Pages 更新

上传后可能需要等待几十秒到几分钟。图片不显示时，先检查文件是否真的在 `images/` 里，文件名和后缀是否完全一致。

## 不要修改代码目录

普通维护者不要修改 `js/` 等代码目录。内容只放在 `content.xlsx`，图片只放在 `images/`。

## 自动链接词条怎么写

如果某些词需要在简介、详情、更新日志里自动变成可点击链接，可以维护 `tele` 表。

推荐列：

- `词条`：正文里会被自动识别的文字，例如 `魔核`
- `导向id`：要跳转到的 `items` 表条目 id，例如 `mat_magic_core`
- `是否展示`：填 `否`、`false`、`no`、`0` 时不启用
- `说明`：只给维护者看，前台不显示

示例：

```text
词条        导向id
魔核        mat_magic_core
魔晶        mat_magic_crystal
紫晶塑体花  plant_zijing_sutihua
```

写好后，正文可以直接写：

```text
掉落魔核、魔晶与宝石。
```

前台展示时会自动把 `魔核`、`魔晶` 变成链接。原来手动写的 `[[魔核]]` 也继续有效。
