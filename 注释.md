# 灵界 Wiki 维护说明

> 内容源已经从 `content.xlsx` 换成 `data.json`。
> **旧的 `content.xlsx` 已不再被网站读取，可以直接删除。**

## 一、普通维护者只需要关注这些

| 文件 / 目录 | 作用 |
| --- | --- |
| `data.json` | **内容总表，唯一内容源。** 改文字、加条目、改分类都在这里 |
| `images/` | 图片文件夹 |
| `灵界设定.docx` | 设定原稿，只读参考，网站不读它 |

代码在 `site/`，普通维护者不要改。

## 二、为什么用 `data.json` 而不是 Excel

- 能用 `git diff` 逐行看清每次改动（Excel 是二进制，只能看出「文件变了」）
- 可以直接从 mod 代码批量生成
- 网页打开时不用再现场解压解析表格，首屏更快
- 代价：**JSON 语法严格**，少一个逗号整站就白屏。所以改完**必须**跑校验（见第七节）

## 三、`data.json` 的结构

顶层是 5 个数组，键名固定不能改：

```json
{
  "site":       [ ... ],
  "sections":   [ ... ],
  "items":      [ ... ],
  "changelog":  [ ... ],
  "tele":       [ ... ]
}
```

### `site` —— 网站标题（只写 1 行）

```json
{ "网站标题": "灵界", "网站版本": "v0.1.0", "作者": "预留作者" }
```

### `sections` —— 左侧卷目分类

```json
{ "分类id": "danyao", "分类名": "丹药与炼制", "排序值": "3", "是否展示": "true" }
```

- `分类id`：英文、数字、下划线、短横线，**不要中文、不要空格**，条目靠它归属
- `排序值`：数字，越小越靠前；`log` 固定 99（更新日志排在最后）
- `是否展示`：填 `false` / `否` / `no` / `0` 时不显示

### `items` —— 条目（物品、机制、Boss 都算条目）

```json
{
  "id": "lj_magic_core",
  "分类id": "cailiao",
  "名称": "魔核",
  "标签": "材料,魔气,合成",
  "图片": "",
  "制作配方": "[images/inventoryimages2/nightmarefuel.png] 噩梦燃料 5、[images/inventoryimages3/twigs.png] 树枝 5",
  "简介": "卡片上显示的一句话。",
  "详情": "点开卡片后的完整内容。\n\n空行分段。",
  "排序值": "1",
  "是否展示": "true"
}
```

| 字段 | 说明 |
| --- | --- |
| `id` | **唯一身份号**，不能重复。游戏里真实存在的物品，直接用 mod 的预制体 id（如 `lj_magic_core`）；机制类条目自拟（如 `realm_system`）。**不要中文** || `分类id` | 必须是 `sections` 里存在的 `分类id`，写错这个条目就**不会显示** |
| `名称` | 展示给玩家的名字 |
| `标签` | 卡片上的小标签，**也会被搜索到**。用中文逗号 `，` 或顿号 `、` 分隔 |
| `图片` | 卡片预览图，只写文件名（见第六节）。留空则卡片不显示图 |
| `制作配方` | 见第五节 |
| `简介` | 卡片外层的简介 |
| `详情` | 点开后的正文，支持换行与插图，见第六节 |
| `排序值` | 同一卷目内数字越小越靠前 |
| `是否展示` | 填 `false` / `否` / `no` / `0` 时隐藏该条目 |

### `changelog` —— 更新日志

**这一栏记的是游戏（mod）自己的版本更新**，不是网站的施工记录。
网站哪天改了什么，看 git 提交历史就行，别写进这里。

```json
{ "日志版本": "v0.1.0", "日期": "2026-06-23", "内容": "新增丹药介绍，修复魔晶配方错误。", "是否展示": "true" }
```

mod 目前没有对外发布的更新日志，所以这一栏是空的（`"changelog": []`），
前台点「更新日志」会显示「暂无条目」，不会报错。等 mod 有版本记录时按上面的格式追加即可。

**注意**：`内容` 会用中文逗号 `，`、顿号 `、`、分号 `;` 拆成多条，前台用「；」连接显示。所以一条改动里不要用这些符号断句。

### `tele` —— 词条自动跳转（只用来登记别名）

**每个条目都会按自己的 `名称` 自动变成跳转词**，正文里写到别的条目名就会自动变成链接，
所以正常情况下**这里的 `tele` 什么都不用加**。

`tele` 只用于「俗称 / 旧名 / 别名」这类情况：

```json
{ "导向id": "lj_butterfly_island", "字段": "魔兽森林", "说明": "", "是否展示": "true" }
```

- `字段`：正文里会被自动识别的别名，可以写多个，用 `，` 或 `、` 分隔
- `导向id`：填目标条目的 `id` **或** `名称`。**填错了不会报错，只会静默失效**，所以务必跑校验
- 同名冲突时 `tele` 覆盖条目名（别名优先）
- 长词优先：`魔核碎片`、`尘火日志` 不会被 `魔核`、`日志` 抢走

> 校验会逐条检查「每个条目名都登记成跳转词」，所以新增条目后如果没链接，
> 跑一遍 `node tools/verify_site.js` 就能看出是哪一条。

## 四、新增一个物品的完整例子

在 `items` 数组里加一项（注意上一项结尾要有逗号）：

```json
{
  "id": "lj_example_pill",
  "分类id": "danyao",
  "名称": "示例丹",
  "标签": "丹药,低阶",
  "图片": "",
  "制作配方": "[images/inventoryimages1/cutgrass.png] 草 3、魔核 1",
  "简介": "低阶丹药，示例用。",
  "详情": "低阶丹药（黄），炼制时间 2 分钟。\n\n效果：示例。",
  "排序值": "99",
  "是否展示": "true"
}
```

## 五、配方怎么写

格式：**`[图标路径] 物品名称 数量`**，多个材料之间用顿号 `、` 分隔。

```text
[images/inventoryimages2/nitre.png] 硝石 2、[images/inventoryimages1/cutreeds.png] 芦苇 5、魔晶 2
```

三条硬规则：

1. **图标路径必须写完整**，而且是**相对于 `index.html`（仓库根目录）**的路径，必须以 `images/` 开头。
   只写 `[nitre.png]` 会去请求 `https://…/lingjie/nitre.png`，**404 且图标空白**。
2. **图标被打散在 `images/inventoryimages1` 到 `inventoryimages4` 四个目录里，没有规律**：硝石在 2、芦苇在 1、树枝在 3。
   路径写错不要紧——跑校验它会直接告诉你正确路径：
   ```text
   x items 第 12 行(lj_xxx)：配方图标不存在 -> images/inventoryimages9/nitre.png；它实际在 images/inventoryimages2/nitre.png
   ```
3. **mod 自己的物品图标已经导出好了**，放在 `images/lingjie/icons/`，文件名就是物品 id。
   所以配方可以直接写 `[images/lingjie/icons/lj_magic_crystal.png] 魔晶 2`。

> 小提示：一般**不要在 `详情` 里再写一遍材料清单**。`制作配方` 字段已经带图标显示了一次，
> 详情再写一遍的话，点开卡片的弹窗里会出现两份重复的物品信息。
> 详情只写炼制时间、成功率、效果、注意事项这些。
>
> 例外：像**阵法**这种自身没有 `制作配方` 的条目，材料（阵眼）只能写在详情里，
> 那就用下面的行内图标写法，一样会显示成「图标 + 名称 + 数量」。

## 五点五、正文里**不要写**什么

这几类话只对维护者有意义，写正文的时候自己注意就行（**不加校验，靠自觉**）。

1. **内部代码名**：预制体 id、脚本路径、常量、字段名
   ❌ `魔核碎片 lj_magic_debris：…`、`长在森林地皮（WORLD_TILES.FOREST）`、`` 攻击周期 1 秒（沿用原版 `BAT_ATTACK_PERIOD`） ``
   ✅ 直接写玩家看到的：`魔核碎片：…`、`长在森林地皮的蜘蛛巢旁`、`攻击周期 1 秒`
   （`images/lingjie/icons/lj_magic_core.png` 这种**图片路径**不算，配方和插图本来就要写）
2. **「mod 里没有 / 没实装 / 与文档不一致」这类批注**
   有问题**问维护者**，不要写进 wiki。真要说「还没有的东西」，用一句人话带过，例如
   `⚠️ 放入虚空戒目前不会解锁任何东西（灵技玩法开发中，敬请期待）。`
3. **设计文档对照**：数值、名称一律以 mod 源码为准，正文里不提「文档写的是…」
4. 需要额外解释时用 `【】`，不要用 `**`（那是 Markdown 语法，站点不渲染）

## 五点六、详情正文怎么写才好看

详情会自动识别三类结构并套上样式（识别是**按写法**来的，所以照下面写就有层级，不用手动加标记）：

| 想写什么 | 怎么写 | 长什么样 |
| --- | --- | --- |
| **小标题** | 单独一行，短标签：`技能`、`击杀掉落`、`每境属性加成（达到该境界后生效）`、`核心强化 —— 生命值` | 左侧金色竖线 + 加粗标题色 |
| **缩进条目** | 行首打一个全角空格 `　`（两层就打两个） | 前面带小圆点的缩进条目 |
| **数据表** | 连续两行以上「`标签` + 全角空格 + 内容」，例如 `凡境　50 · 100 · …` | 带边框的表格：左侧标签列 + 右侧数值列（数字等宽对齐），隔行浅底色 |

几条经验：

- **别把一大堆数字塞成一整段**，按上面第三行写成「一境一行」，读起来轻松很多
- 小标题**前后留空行**会更清晰；标题紧跟内容行也可以（会自动分开）
- 正文里**不要手打空格来对齐**：全角空格只在行首用来表示缩进层级，表格列宽由样式负责
- 正文里**提到别的条目**（例如 `对应异火：龙炎心火。`）会自动变成**可点击的链接**，名字前面还会自动挂上该条目的**小图标**。
  **直接写名字就行**，不用手写图标；没有图标的条目自动只显示名字，不会留破图
- 正文里写**材料名 + 数量**（例如 `击杀掉落 怪物肉 4、橙宝石 2`、`摧毁后返还 2 木板`、`留下 5 个木炭`）
  也会**自动补上材料图标**（原版材料走 `images/inventoryimages*/`，mod 材料走 `images/lingjie/icons/`）——
  同样**不用手写 `[图片路径]`**。只在「名字紧挨着数量」时补，纯叙述里提到材料名不会乱加图标；
  本身是条目的名字（会自动带链接图标）不会重复补
- 一段里的普通说明文字照常写，样式不会乱加（规则保守，拿不准就当普通文字）

> 校验会检查「结构化渲染后没有丢字」，所以改完跑一遍 `node tools/verify_site.js` 就放心。

## 六、图片

### 卡片预览图（`图片` 字段）

- 只写**文件名**，例如 `1234.png`，程序会到 `images/` 下找
- 也可以写 `images/xxx/yyy.png` 或 `https://…` 外链
- 写多个会以轮播显示，用 `，` 或 `、` 分隔
- **留空 = 卡片不显示图片**（不会出现破图）

### 图片目录怎么放

| 目录 | 放什么 | 命名 |
| --- | --- | --- |
| `images/lingjie/icons/` | **灵界 mod 的物品图标**（64×64，给配方用） | **物品预制体 id**，例如 `lj_magic_crystal.png` |
| `images/lingjie/showcase/` | **展示图**（物品 idle 动画第一帧，给卡片用） | 同上，`<物品 id>.png` |
| `images/lingjie/anim/` | **补充动画帧**（技能特效、状态徽章，给正文插图用） | 内容名，例如 `poison_swamp.png` |
| `images/inventoryimages1~4/` | 《饥荒》原版物品图标 | 原版文件名，例如 `nitre.png` |
| `images/`（根） | 其他杂图 | 英文小写 |

> `showcase/` 里有一张例外：`lj_remains_altar_complete.png` 是残骸祭坛的**完成体**，
> 只给详情正文当插图用（不是卡片图，来源见该目录 README）。

**卡片图优先用展示图，没有展示图时自动回退到图标**（都由脚本按物品 id 自动挂上，不用手填）。

图标已经全部导出好了（74 个）。**新增 mod 物品时，把同名 PNG 放进 `images/lingjie/icons/` 即可**，
`data.json` 里的配方和卡片图就能直接用。

### 详情里插图

**必须单独占一段**（前后留空行），否则会原样显示成文字：

```text
这是第一段。

[[图片:1234.png|图片说明]]

这是第二段。
```

也支持英文写法 `[[image:1234.png|caption]]`。**不要在内容里插入图片对象**，网站只读文字。

### 详情里的行内小图标

和配方一个写法：**单个中括号里放图片路径**，可以夹在句子中间，会渲染成 20px 的小图标。
只有图片扩展名（`.png` / `.jpg` / `.jpeg` / `.webp` / `.gif` / `.svg`）才会被当成图标，别的方括号内容照旧是文字。

```text
阵眼：四季核心（[images/inventoryimages1/deerclops_eyeball.png] 巨鹿眼球 1、[images/lingjie/icons/lj_magic_core.png] 魔核 1）。
```

显示效果就是「图标 名称 数量」串在正文里，与卡片上的配方栏一致。
文件不存在时图标会自动隐藏，不会出现破图。

> 三种图片写法不要混：`[路径]` 是行内小图标、`[[图片:路径]]` 是独占一段的大图、
> `[[条目名]]` 是指向另一张卡片的跳转链接。

### 图片文件名要求

- 推荐英文小写、数字、短横线、下划线
- 不建议中文、空格、括号、特殊符号
- 文件名大小写必须和 `data.json` 里完全一致
- `.jpg` 和 `.png` 是不同文件
- mod 物品图标命名为**物品 id**，例如 `lj_magic_crystal.png`，放到 `images/lingjie/icons/`（74 个已经导好了）

## 七、改完一定要校验

```bash
python tools/validate_content.py    # 查 JSON 结构、id 重复、分类失配、词条死链、图标路径
node   tools/verify_site.js         # 用网站真实代码跑一遍，确认前台能正常读出来
```

两条都通过再上传，基本不会把站改坏。

## 八、`tools/` 里的脚本

| 脚本 | 作用 |
| --- | --- |
| `validate_content.py` | 内容校验（必跑）：JSON 结构、id 重复、分类失配、词条死链、配方图标路径 |
| `verify_site.js` | 用真实站点代码验证（必跑）：卷目/条目数、词条跳转、配方图标、**详情正文行内图标是否真的渲染出来**、卡片图是否存在 |
| `build_content.py` | 批量重建 `data.json` 的生成器，内含全部条目文字。**只在需要整体重建时用**；平时改文字直接改 `data.json` |
| `extract_mod_icons.py` | 从 mod 的图标图集（`.tex` + `.xml`）切出 64×64 图标 |
| `extract_mod_showcase.py` | 生成导出展示图用的 Lua 脚本（配合下面的 `run_dst_app.ps1`） |
| `export_altar_stage2.lua` | 单独导出残骸祭坛「修复完成」形态（`idle_stage2`）当详情正文插图，配合同一个 `run_dst_app.ps1` |
| `export_sword_held.lua` | 单独导出淬铁灵剑的**手持外观**（`BUILD` 动画第 1 帧），给鼠标光标当素材 |
| `make_sword_cursor.py` | 把上面导出的手持图裁边、缩放、转 45°，生成 `images/cursor_sword.png`（默认剑长 76px，可传第三个参数改大小） |
| `run_dst_app.ps1` | 跑 DST Mod Tool 的壳：**强制断网**、跑完清掉常驻进程、解析它的 JSON 报告 |

## 九、GitHub Pages 更新

上传后可能需要等待几十秒到几分钟。图片不显示时，先检查文件是否真的在 `images/` 里，路径、文件名、后缀是否完全一致。

## 十、不要修改代码目录

`site/` 是网站代码，普通维护者不要改。内容只放在 `data.json`，图片只放在 `images/`。

**例外：侧边栏底部的「加入 mod 讨论群」按钮**在 `site/app.js` 顶部，是留好的可改项：

```js
const QQ_GROUP_NUMBER = "767318372";           // 群号
const QQ_GROUP_NAME = "【灵界】渡劫办事处";      // 群名（显示在按钮下面）
const QQ_GROUP_INVITE_URL = "https://qun.qq.com/universal-share/share?...";  // 群分享链接
```

- 换群：三个都改（或者只改群号 + 换成新群的分享链接）
- **分享链接是从 QQ 群设置里「分享群链接」复制的**，任何浏览器点按钮都会打开加群页面
  （手机端会引导到 QQ 里申请）；里面的 `authKey` 一类参数由 QQ 生成，**可能过期**——
  万一哪天按钮点出来是失效页面，重新复制一次换上就行
- 链接留空时会退回用 `mqqapi://...card_type=group&uin=<群号>` 唤起 QQ 群卡片，
  但那只在装了 QQ 的手机/电脑上有效（桌面浏览器点了只会看到群号）
- 不管哪种情况，按钮下面都会显示「群名 + 群号 + 复制群号」，方便玩家手动搜群

**例外：全站鼠标光标**是 `site/styles.css` 顶部 `:root` 里的两行变量（淬铁灵剑手持图）：

```css
--cursor-sword:url("../images/cursor_sword.png") 4 4, auto;      /* 页面默认光标 */
--cursor-sword-btn:url("../images/cursor_sword.png") 4 4, pointer; /* 按钮/链接，图挂了退回小手 */
```

- 换图：把 `images/cursor_sword.png` 换掉即可（尺寸 ≤128×128，`4 4` 是热点坐标，改成新图的剑尖位置）
- 换回系统光标：删掉 `html{cursor:var(--cursor-sword)}` 这一行就行
- 输入框故意保留 `text`（I 形光标），不然打字找不到插入点

