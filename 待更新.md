# 待更新清单（mod 已改、但等游戏更新后再同步进 wiki）

> 维护者用，不参与站点渲染。
>
> 情况：作者给了一个**比 v0.6 更新的包**，但**游戏本体还没更新**，
> 作者要求「先保留，等游戏更新一起更新」。所以 wiki 目前**保持 v0.6 状态不变**，
> 这份文件只用来记住待办的改动，等游戏更新后照着改。
>
> 对照来源：
> · 已发布基线 = `lj_mod (2).zip`（version 0.6，内部最新时间 2026-09-22 19:38）
> · 待发布包   = `lj_mod.zip`（内部最新时间 2026-09-23 00:20；`modinfo` 里版本号**仍是 0.6**）
>   路径：D:\aideepseek\data\attachments\v1\files\7f\7fe18835f0da778ebdc87d4037319cb6d6c986204b70d0eefef3e77054cc3899\lj_mod.zip
> 文件差异：新增 1 个、改动 10 个（其中 lj_chiyan_scorpion_dragon.lua 只是换行符，内容没变）

---

## 待同步的 5 项内容改动

| # | 卡片 | 要改成什么 | 代码依据 |
|---|---|---|---|
| 1 | **驱灵锁魔阵** | 补「**敌对生物出生在阵内会被立即驱逐到阵外最近的位置**；落点规则：陆生生物只能落陆地，水生与飞行可以落海面，不能落在洞穴口旁边」 | `lj_formation_manager.lua` `EvictSpawnedHostile` / `CanPlaceHostileOutsideWard`、`formations/util.lua` `IsHostile`、`formations/geometry.lua` `GetOutsideCandidates`、`formations/register.lua` 钩子改为所有带战斗+生命的非玩家实体 |
| 2 | **境界体系**（经验获取那段） | 补「**亚比盖尔与薇洛技能火焰造成的击杀也算主人的参战**（其他随从不算）」 | `main/player/realm_combat.lua`：新增 `GetExperiencePlayer`（abigail → follower/_playerlink）与 `RecordWillowIgnite`（willow 点燃时记录） |
| 3 | **紫晶塑体花** | 补「**每个世界保证有一朵**：在曼德拉草的家（MandrakeHome）节点里找空地生成；**旧存档进世界后会补种**；花异常消失也会补回来；生成状态会存档」 | 新组件 `components/lj_purple_magic_flower_worldgen.lua` + `main/world/resources.lua` 给 forest 挂载 + `lj_purple_magic_flower.lua` 里 `MarkGenerated()` |
| 4 | **深度入魔** | 魔气反弹补一句「**单次最多反弹 75 点**」 | `demonization.lua`：`DoDelta(-math.min(damage * 0.05, 75), …)`（原为无上限的 `damage * 0.05`） |
| 5 | **入魔总览** | 「不会被入魔的」名单里**加上海洋影怪** | `demonization.lua` 的 `prefabList` 新增 `oceanhorror` |
| 6 | 更新日志 | 图标 / 视觉类改动也**要写进日志**，统一用「**优化视觉**」这种说法。本次对应：祭天雷劫的落点预警图标（`reticuleaoehostiletarget` → `reticuleaoeghosttarget`）→ 日志里写「**优化视觉（祭天雷劫预警图标）**」 | 同下 |

## 不必写进 wiki 的

- `demonization.lua` 里新加了一行被注释掉的 `--warning.entity:SetCanSleep(false)`
  —— 死代码，不影响行为。
- `lj_chiyan_scorpion_dragon.lua` 只是换行符从 LF 变成 CRLF，内容没变。

（作者指示：**图标、特效这类视觉改动不要当成内部实现丢掉**，要在更新日志里以
「优化视觉」的说法记一笔 —— 参见 v0.6 日志里「灵虚葫：优化葫芦部署后的视觉逻辑」那条。）

## 同步时要一起做的

- 更新日志：等游戏更新后按上面的改动写一条新日志（版本号以那时的 `modinfo` 为准；
  这个包的 `modinfo` 仍是 0.6，所以**先不写**）。
- 同步完记得把「入魔总览」的名单改动回填 `待确认.md` 里那条（不会被入魔的名单）。
