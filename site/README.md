# 灵界资料站维护说明

普通维护者只看这里：

1. 改内容：修改并上传仓库一级目录的 `content.xlsx`。
2. 加图片：上传图片到仓库一级目录的 `images/`，然后在 Excel 里写图片文件名。
3. 不要在 Excel 单元格里直接插入图片对象。
4. 不要改代码文件。
5. 不要动 `js/` 等代码目录。

详细说明见 `docs/content-xlsx-workflow.md`。
