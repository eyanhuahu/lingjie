# 待确认清单

> 这是**维护者用**的核对清单，不是给玩家看的内容，不参与站点渲染。
>
> 做法：按 wiki 卷目顺序逐个模块把「wiki 写的」和「mod 最新代码」对照
> （对照对象：`lj_mod (1).zip`，即 mod v0.3）。
> 凡是**代码里没能直接确认**的说法都记到这里，等作者确认后再回填 wiki；
> 已确认一致的项不记录（各模块的报告见 git 提交信息）。

**核对进度：13 个卷目全部走完**（①境界 ②生物入魔 ③丹药与炼制 ④精炼材料 ⑤建筑
⑥武器与盔甲 ⑦法宝与工具 ⑧阵法灵技 ⑨灵植 ⑩墟火与异火 ⑪地形与Boss ⑫人物（空）
⑬更新日志）。

---

## 一、待作者确认（wiki 有说法，但代码里没找到定义处）

| # | 模块 | 条目 | wiki 现在写的 | 我在代码里看到的 | 需要确认什么 |
|---|---|---|---|---|---|
| 2 | ② 生物入魔 | 入魔总览 | 「不会被入魔的：影怪系、被动小动物、玩家、同伴、墙体、装备展示模型」 | `demonization.lua:1761` 明确排除：影体分身、`HasTag("swc2hm")`、无生命组件、已死亡；`:1770` 普通生物被玩家雇佣则不入魔 | 其余几类的排除点在「入魔组件的挂载白名单」里，我还没定位到那个挂载点。名单本身对吗？ |
| 3 | ① 境界（mod 侧） | 魂幡炼制时间 | wiki 没写（这是对的） | `lj_alchemy_defs.lua:139`：`time = TUNING.ETHEREAL_REALM.SOUL_BANNER_ALCHEMY_TIME`，但 `tuning.lua` 里没有这个常量，全库只有这一处引用 → 值为 nil | 是**漏定义**吗？本来想给几分钟？ |
| 4 | ④ 精炼材料 | 魔核 / 魔晶 / 蝎龙骨 / 紫晶壳 / 狮骨 / 噬魂蛇鳞片 等 | 「**无法摧毁**」「无法摧毁、可分解」这类否定说法 | 材料预制体散在各自相关文件里（蝎龙骨在炽岩蝎龙文件、紫晶壳在紫晶塑体花文件、狮骨在月蚀晶翼狮文件、鳞片在噬魂蛇文件），没有逐个确认可烧毁/可分解设置 | 这些「无法摧毁 / 可分解」的写法对吗？ |
| 5 | ⑥ 武器与盔甲 | 灵韵 / 星陨 | 暴击**伤害** ×1.8 / ×2；耐久 200 / 300；冰冻阈值「小型 2 层=2 秒、中型 3 层=1.5 秒、大型 5 层=1 秒」；星陨充能「每 5 次 25%、满 100% 进星陨状态、停手 10 秒归零、星陨状态每击 +5 伤」 | 已确认：`SetDamage(78/88)`、`SetRange(10)`、暴击率 `math.random() < 0.2 / 0.22`、冰蚀与焚灼 `duration=8 interval=0.5` 16 点、陨火刺 `SetDamage(140)`、灵力消耗 20/10、击杀 +5 | 暴击倍率、耐久、冰冻阈值、充能这几项没找到定义处（可能在共享战斗 hook / 组件里）。数字对吗？ |
| 6 | ⑥ 武器与盔甲 | 淬铁灵剑 / 骸龙甲 / 晶羽冠 | 淬铁灵剑「手持 +10% 移速、1 碎片修 10 耐久、耐久 100」；防具「1 魔晶 +1% 防御最高 90、1 位面防御最高 10、血蝠精血恢复满耐久」 | 已确认：`SetDamage(45)`、`InitCondition(ARMOUR_DURABILITY=1500, 0.8)`、以血修甲全部参数（`interval=2 / delay=5 / cost=1 / amount=10 / min_health=0.3`） | 移速、碎片修复量、魔晶/位面防御上限没找到定义处。数字对吗？ |
| 7 | ⑦ 法宝与工具 | 灵虚葫 | 跟随状态下「**16 码**范围内」没主人的异火会自动飞进葫芦 | 组件与 proxy 里只看到 `ABSORB_SPEED = 10`、`ABSORB_ARRIVE_DIST = 1.2`、`MUST_TAGS = { "lj_flame" }`；**收集半径的 16 没找到** | 16 码这个数字对吗？定义在哪个文件？ |
| 8 | ⑦ 法宝与工具 | 魂幡 | 「会自动吸收原版的 **4 种**影怪」 | 白名单只抓到 `terrorbeak` / `crawlinghorror` / `crawlingnightmare`（第 4 行被我的正则漏掉）+ `ONEOF_TAGS = { shadowcreature, nightmarecreature, lj_can_be_refined }` | 第 4 种是哪个？另外「影骑士/影主教/影车不会被吸」与白名单一致 ✓ |
| 9 | ⑪ 地形与 Boss | 炽岩蝎龙 | 「护甲 30」 | 已确认：`SetMaxHealth(30000)`、`for i = 1, 4`（召唤 4 只）、`summon = 30` / `charge = 30`、爪击计数上限 4、`_lj_summon_thresholds["10000"]`、幼虫 `SetMaxHealth(250)`；**护甲 30 没找到** | 护甲 30 对吗？（可能在物理护甲组件里）|
| 10 | ⑪ 地形与 Boss | 妖蝠 / 暗影血蝠 | 妖蝠「300 血 / 25 伤 / 攻击周期 1 秒」；暗影血蝠「2000 血 / 55 伤 / 音波半径 8、每 0.5 秒脉冲、持续 9 秒、冷却 30 秒、半血后每 15 秒召唤 1 只妖蝠」 | 两款都定义在 `scripts/prefabs/creatures/lj_bats.lua`，这次没读它的数值；巢穴那边只确认了 `DEMON_BAT_REGEN = 2700`（45 分钟 ✓）| 上面那些数字对吗？ |

## 二、可选补充（代码里明确有、wiki 没写，看要不要加）

| # | 模块 | 条目 | 代码里的事实 | 建议 |
|---|---|---|---|---|
| 1 | ② 生物入魔 | 中度 / 深度入魔 | 半血护盾的特效是**红色力场**：`fx.AnimState:SetMultColour(1, 0.15, 0.15, 1)` + `fx.Light:SetColour(1, 0.15, 0.15)` | 要不要在「限定易伤」后面补一句「护盾是红色力场」？ |
| 2 | ⑦ 法宝与工具 | 月魄凝液瓶 | 有**祈雨**功能：`UseMoonRain` 消耗 `RAIN_COST = 20` 点灵液，触发后世界设为一直降水、**10 分钟后恢复**；`SetMaxUses(100)` | wiki 的月魄凝液瓶卡片完全没写祈雨 —— 这是玩家能用的功能，建议补 |

## 三、已解决（留档，不必再问）

| 模块 | 原来写的 | 处理 |
|---|---|---|
| ② 生物入魔 | 入魔总览「回血：脱离有效目标连续 10 秒后，每 5 秒回复最大生命的 2%…」 | 作者指示**删掉** → 中英两句都已删除（`regen_delay=10` 与每跳 2% 在代码里是有的，但那个 5 秒间隔没找到定义处，作者决定不写）|
| ① 境界 | 经验 = 目标**入魔前**的最大生命 × 5% | 按代码改成「目标的最大生命 × 5%；按代码取的是当前最大生命，被入魔抬高的那部分也算在内」（`1d8ac54`）|
| ④ 精炼材料 | 魔核 / 魔晶「可在 &lt;灵界&gt;、&lt;精炼材料&gt; 栏位制作」 | 代码 filters 是 `ETHEREAL_REALM, MAGIC, REFINE` → 改成「&lt;灵界&gt;、&lt;魔法&gt;、&lt;精炼&gt;」（`7855b98`）|
| ⑨ 灵植 | 彼岸花「全季节生长」 | 代码有 `MakeNoGrowInWinter` → 改成「冬季不生长（与融灵草一样）」（`15d9d29`）|
| ⑩ 墟火 | 异火主人「落地后只有主人能捡」/「收进容器成为新主人」 | 代码是「首次掉落无主、只有葫芦吸收才定主人」→ 已订正（`00983e3`）|
| ⑩ 墟火 | 葫芦吸收「每秒扣 1 灵力值」 | 代码只有每秒 2 点生命 + 点燃一次 → 已订正（`00983e3`）|
| ⑩ 墟火 | 焚毁「10 秒后每 2 秒消耗 1、从第一格开始、多个异火倍速」 | 代码是「每 10 秒随机焚毁 1 件、多个异火限流不加速」→ 已订正（`c1d37e4`）|
| ⑩ 墟火 | 风干的羽毛「右键阅读」 | 它是炼丹炉材料，纸条在尘火日志里读 → 已订正（`c231bb6` / `ec1fd28`）|
| ⑩ 墟火 | 4 张线索卡「右键打开看纸条」 | 弄开是变成对应日志 → 纸条原文搬到日志卡（`ec1fd28`）|
| ⑪ 地形 | 蝴蝶岛「三棵阴郁之棘」等具体数字 | 按要求改模糊说法；实体数量按代码订正（夜蝠巢穴 4→3、正常的树 2→1）（`b0bca70` / `4d5c946`）|
| ③ 丹药 | 清心丸卡片没写「可解噬魂蛇毒」 | 已加「额外用途：可以解毒——服用后直接解除噬魂蛇毒」|
| ③ 丹药 | 炼丹总则没写「强异火必定成功、且不改变丹劫判定」 | 已加 |
| ③ 丹药 | 炼丹总则没写「异火本体炼丹不消耗」 | 已加「只有分裂出来的火焰才是消耗品，会消耗 1 个」|
| 全站 | 卡片里混入内部说明（HUD 根节点 / player_classified / 版本对比） | 已清掉（`436d45c`）|
