# 待确认清单

> 这是**维护者用**的核对清单，不是给玩家看的内容，不参与站点渲染。
>
> 做法：按 wiki 卷目顺序逐个模块把「wiki 写的」和「mod 最新代码」对照
> （对照对象：`lj_mod (1).zip`，即 mod v0.3）。
> 凡是**代码里没能直接确认**的说法都记到这里，等作者确认后再回填 wiki；
> 已确认一致的项不记录（各模块的报告见 git 提交信息）。

**核对进度：13 个卷目全部走完**（①境界 ②生物入魔 ③丹药与炼制 ④精炼材料 ⑤建筑
⑥武器与盔甲 ⑦法宝与工具 ⑧阵法灵技 ⑨灵植 ⑩墟火与异火 ⑪地形与Boss ⑫人物（空）
⑬更新日志）。

---

## 一、待作者确认（**作者已答复：这些不用改，wiki 保持现状** —— 仅留档备查）

> 下面这些都是「wiki 有写法、但我在代码里没找到定义处」的项。
> 作者 2025 年的答复：**目前不改，维持现有 wiki 状态**。
> 若以后要动，再逐条回填即可。

| # | 模块 | 条目 | wiki 现在写的 | 我在代码里看到的 | 需要确认什么 |
|---|---|---|---|---|---|
| 1 | ② 生物入魔 | 入魔总览 | 「不会被入魔的：影怪系、被动小动物、玩家、同伴、墙体、装备展示模型」 | `demonization.lua:1761` 明确排除：影体分身、`HasTag("swc2hm")`、无生命组件、已死亡；`:1770` 普通生物被玩家雇佣则不入魔 | 其余几类的排除点在「入魔组件的挂载白名单」里，我还没定位到那个挂载点。名单本身对吗？ |
| 2 | ⑥ 武器与盔甲 | 灵韵 / 星陨 | 暴击**伤害** ×1.8 / ×2；耐久 200 / 300；冰冻阈值「小型 2 层=2 秒、中型 3 层=1.5 秒、大型 5 层=1 秒」；星陨充能「每 5 次 25%、满 100% 进星陨状态、停手 10 秒归零、星陨状态每击 +5 伤」 | 已确认：`SetDamage(78/88)`、`SetRange(10)`、暴击率 `math.random() < 0.2 / 0.22`、冰蚀与焚灼 `duration=8 interval=0.5` 16 点、陨火刺 `SetDamage(140)`、灵力消耗 20/10、击杀 +5 | 暴击倍率、耐久、冰冻阈值、充能这几项没找到定义处（可能在共享战斗 hook / 组件里）。数字对吗？ |
| 3 | ⑥ 武器与盔甲 | 淬铁灵剑 / 骸龙甲 / 晶羽冠 | 淬铁灵剑「手持 +10% 移速、1 碎片修 10 耐久、耐久 100」；防具「1 魔晶 +1% 防御最高 90、1 位面防御最高 10、血蝠精血恢复满耐久」 | 已确认：`SetDamage(45)`、`InitCondition(ARMOUR_DURABILITY=1500, 0.8)`、以血修甲全部参数（`interval=2 / delay=5 / cost=1 / amount=10 / min_health=0.3`） | 移速、碎片修复量、魔晶/位面防御上限没找到定义处。数字对吗？ |
| 4 | ⑦ 法宝与工具 | 灵虚葫 | 跟随状态下「**16 码**范围内」没主人的异火会自动飞进葫芦 | 组件与 proxy 里只看到 `ABSORB_SPEED = 10`、`ABSORB_ARRIVE_DIST = 1.2`、`MUST_TAGS = { "lj_flame" }`；**收集半径的 16 没找到** | 16 码这个数字对吗？定义在哪个文件？ |
| 5 | ⑦ 法宝与工具 | 魂幡 | 「会自动吸收原版的 **4 种**影怪」 | 白名单只抓到 `terrorbeak` / `crawlinghorror` / `crawlingnightmare`（第 4 行被我的正则漏掉）+ `ONEOF_TAGS = { shadowcreature, nightmarecreature, lj_can_be_refined }` | 第 4 种是哪个？另外「影骑士/影主教/影车不会被吸」与白名单一致 ✓ |
| 6 | ⑪ 地形与 Boss | 妖蝠 / 暗影血蝠 | 妖蝠「300 血 / 25 伤 / 攻击周期 1 秒」；暗影血蝠「2000 血 / 55 伤 / 音波半径 8、每 0.5 秒脉冲、持续 9 秒、冷却 30 秒、半血后每 15 秒召唤 1 只妖蝠」 | 两款都定义在 `scripts/prefabs/creatures/lj_bats.lua`，这次没读它的数值；巢穴那边只确认了 `DEMON_BAT_REGEN = 2700`（45 分钟 ✓）| 上面那些数字对吗？ |
| 7 | ⑩ 墟火与异火 | 冰霜业火 / 龙炎心火 / 狂鬃焰 / 灵煊尘火 / 紫鳞妖焰 | 原来是「无法摧毁、**可分解**」 | 按代码统一改成「无法摧毁、分解」（`3aaad2a`）：全库只有月魄凝液瓶有 `AddDeconstructRecipe`，异火本体没有任何分解入口，其它配方都是 `no_deconstruction = true` | 异火要不要保留「可分解」的写法？（若打算给它们加分解返还，我就改回来）|
| 8 | ③ 丹药与炼制 | 丹药品阶与丹劫 | 「丹药分**四阶**：低阶（黄）、中阶（玄）、高阶（地）、**天阶（天）**」 | 最新包的 `lj_alchemy_defs.lua` 里 `TIER_DATA` 只有 **3 档**：`low`（2 分钟 / 50%）、`mid`（4 分钟 / 50% / 丹劫 10 秒）、`high`（8 分钟 / 20% / 丹劫 8 秒）；全库没有「天阶」相关代码或配方 | 「天阶（天）」是**已规划但未实装**，还是写法有误？ |
| 9 | ③ 丹药与炼制 | 凝气丸 | 「恢复 50 饱食度，**猪人也爱吃**」 | `lj_pill.lua` 里只有 `lj_ningqi_pill = { hunger = 50, tradable = true }`，`edible.foodtype = FOODTYPE.GOODIES`；没有专门给猪人的标记 | 「猪人也爱吃」是实测效果还是描述性说法？（不影响准确性，只是我核不到代码依据）|

## 二、可选补充（代码里明确有、wiki 没写，看要不要加）

| # | 模块 | 条目 | 代码里的事实 | 建议 |
|---|---|---|---|---|
| — | — | — | 暂无（原来的两条作者已同意补上，见「已解决」） | — |

## 三、已解决（留档，不必再问）

| 模块 | 原来写的 | 处理 |
|---|---|---|
| ② 生物入魔 | 中度 / 深度入魔没写护盾外观 | 代码里护盾是红色力场（`SetMultColour(1, 0.15, 0.15, 1)` + `Light:SetColour(1, 0.15, 0.15)`）→ 作者同意补上，两张卡都加了「护盾表现为红色力场。」|
| ⑦ 法宝与工具 | 月魄凝液瓶没写祈雨 | 代码有 `UseMoonRain`（动作名 `LJ_MOON_VASE_RAIN`「祈雨」）消耗 20 点灵液、世界持续降水、10 分钟后恢复、`SetMaxUses(100)` → 作者同意补上，已加一整段 |
| ② 生物入魔 | 入魔总览「回血：脱离有效目标连续 10 秒后，每 5 秒回复最大生命的 2%…」 | 作者指示**删掉** → 中英两句都已删除 |
| ① 境界 | 经验 = 目标**入魔前**的最大生命 × 5% | 按代码改成「目标的最大生命 × 5%；按代码取的是当前最大生命，被入魔抬高的那部分也算在内」（`1d8ac54`）|
| ④ 精炼材料 | 魔核 / 魔晶「可在 &lt;灵界&gt;、&lt;精炼材料&gt; 栏位制作」 | 代码 filters 是 `ETHEREAL_REALM, MAGIC, REFINE` → 改成「&lt;灵界&gt;、&lt;魔法&gt;、&lt;精炼&gt;」（`7855b98`）|
| ④ 精炼材料 | 魔核 / 魔晶 / 5 张异火卡「无法摧毁、**可分解**」 | 逐个查了预制体：这些文件里没有任何 `deconstructable`，全库只有月魄凝液瓶有 `AddDeconstructRecipe` → 统一改成「无法摧毁、分解」（`3aaad2a`）；异火那 5 张要不要保留原写法见「待作者确认」第 9 条 |
| ① 境界 / ⑪ 地形 | 魂幡炼制时间常量未定义、炽岩蝎龙「护甲 30」 | mod v0.6 已修：`SOUL_BANNER_ALCHEMY_TIME = 4 * 60`（魂幡炼制 4 分钟）写入 tuning.lua；蝎龙 `SetAbsorptionAmount(.3)` → `.15`（护甲 30% → 15%）。wiki 已同步 |
| ⑨ 灵植 | 彼岸花「全季节生长」 | 代码有 `MakeNoGrowInWinter` → 改成「冬季不生长（与融灵草一样）」（`15d9d29`）|
| ⑩ 墟火 | 异火主人「落地后只有主人能捡」/「收进容器成为新主人」 | 代码是「首次掉落无主、只有葫芦吸收才定主人」→ 已订正（`00983e3`）|
| ⑩ 墟火 | 葫芦吸收「每秒扣 1 灵力值」 | 代码只有每秒 2 点生命 + 点燃一次 → 已订正（`00983e3`）|
| ⑩ 墟火 | 焚毁「10 秒后每 2 秒消耗 1、从第一格开始、多个异火倍速」 | 代码是「每 10 秒随机焚毁 1 件、多个异火限流不加速」→ 已订正（`c1d37e4`）|
| ⑩ 墟火 | 风干的羽毛「右键阅读」 | 它是炼丹炉材料，纸条在尘火日志里读 → 已订正（`c231bb6` / `ec1fd28`）|
| ⑩ 墟火 | 4 张线索卡「右键打开看纸条」 | 弄开是变成对应日志 → 纸条原文搬到日志卡（`ec1fd28`）|
| ⑪ 地形 | 蝴蝶岛「三棵阴郁之棘」等具体数字 | 按要求改模糊说法；实体数量按代码订正（夜蝠巢穴 4→3、正常的树 2→1）（`b0bca70` / `4d5c946`）|
| ③ 丹药 | 清心丸卡片没写「可解噬魂蛇毒」 | 已加「额外用途：可以解毒——服用后直接解除噬魂蛇毒」|
| ③ 丹药 | 炼丹总则没写「强异火必定成功、且不改变丹劫判定」 | 已加 |
| ③ 丹药 | 炼丹总则没写「异火本体炼丹不消耗」 | 已加「只有分裂出来的火焰才是消耗品，会消耗 1 个」|
| 全站 | 卡片里混入内部说明（HUD 根节点 / player_classified / 版本对比） | 已清掉（`436d45c`）|
