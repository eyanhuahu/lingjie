# 待确认清单

> 这是**维护者用**的核对清单，不是给玩家看的内容，不参与站点渲染。
>
> 做法：按 wiki 卷目顺序逐个模块把「wiki 写的」和「mod 最新代码」对照。
> 凡是**代码里没能直接确认**的说法都记到这里，等作者确认后再回填 wiki。
> 已确认一致的项不记录（各模块的报告里有）。

最后更新：模块③ 丹药与炼制 查完（待推送 commit 见 git log）

---

## 一、待作者确认（wiki 有说法，但代码里没找到/没找到统一入口）

| # | 模块 | 条目 | wiki 现在写的 | 我在代码里看到的 | 需要确认什么 |
|---|---|---|---|---|---|
| 1 | ② 生物入魔 | 入魔总览 | 脱战后「**每 5 秒**回复最大生命的 2%」 | `lj_demonization_defs.lua`：`regen_delay = 10`（脱战 10 秒）；`demonization.lua:532`：每跳 `maxhealth * 0.02` | 那个 **5 秒**的定时器我**没找到**（可能在别的文件里）。5 秒这个数字对吗？ |
| 2 | ② 生物入魔 | 入魔总览 | 「不会被入魔的：**影怪系、被动小动物、玩家、同伴、墙体、装备展示模型**」 | `demonization.lua:1761` 明确排除：影体分身（`_lj_is_shadow_clone`）、`HasTag("swc2hm")`、无生命组件、已死亡；`:1770` 普通生物被玩家雇佣则不入魔 | 「被动小动物 / 玩家 / 同伴 / 墙体 / 装备展示模型」这几类的**排除点**在代码里是**入魔组件的挂载白名单**，我还没定位到那个挂载点。名单本身对吗？ |
| 3 | ① 境界 | （mod 侧） | wiki 没写魂幡的炼制时间（是对的） | `lj_alchemy_defs.lua:139`：`time = TUNING.ETHEREAL_REALM.SOUL_BANNER_ALCHEMY_TIME`，而 `tuning.lua` 里**没有**这个常量，全库只有这一处引用 → 值为 nil | 是**漏定义**吗？本来想给几分钟？ |

| 4 | ④ 精炼材料 | 魔核 / 魔晶 / 蝎龙骨 / 紫晶壳 / 狮骨 / 噬魂蛇鳞片 等 | 「**无法摧毁**」「无法摧毁、可分解」这类否定说法 | 材料的预制体定义散在各自相关文件里（蝎龙骨在炽岩蝎龙文件、紫晶壳在紫晶塑体花文件、狮骨在月蚀晶翼狮文件、鳞片在噬魂蛇文件），我这次**没有逐个确认**它们的可烧毁/可分解设置 | 这些「无法摧毁 / 可分解」的写法对吗？（需要逐个 prefab 看 `MakeSmallBurnable` / `MakeDeconstructable` 之类） |
| 5 | ⑥ 武器与盔甲 | 灵韵 / 星陨 | 暴击**伤害** ×1.8 / ×2；耐久 200 / 300；冰冻阈值「小型 2 层 = 冻结 2 秒、中型 3 层 = 1.5 秒、大型 5 层 = 1 秒」；星陨充能「每 5 次 25%、满 100% 进星陨状态、停手 10 秒归零、星陨状态每击 +5 伤」 | 已确认：`SetDamage(78/88)`、`SetRange(10)`、暴击**率** `math.random() < 0.2 / 0.22`、冰蚀/焚灼 `duration=8 interval=0.5` 16 点、陨火刺 `SetDamage(140)`、灵力消耗 20/10、击杀 +5 | 上面那几项**没找到定义处**（暴击倍率和耐久可能在共享的战斗 hook / 组件里，我这次没定位到）。数字对吗？ |
| 6 | ⑥ 武器与盔甲 | 淬铁灵剑 / 骸龙甲 / 晶羽冠 | 淬铁灵剑「手持 +10% 移速、1 魔核碎片修 10 点耐久、耐久 100」；防具「1 魔晶 +1% 防御最高 90、1 位面防御最高 10、血蝠精血恢复满耐久」 | 已确认：`SetDamage(45)`、`InitCondition(ARMOUR_DURABILITY=1500, 0.8)`、以血修甲全部参数（`interval=2 / delay=5 / cost=1 / amount=10 / min_health=0.3`）| 移速、修复速率、魔晶/位面防御的**上限**没找到定义处。数字对吗？ |
| 7 | ⑦ 法宝与工具 | 灵虚葫 | 跟随状态下「**16 码**范围内」没主人的异火会自动飞进葫芦 | 组件与 proxy 里只看到 `ABSORB_SPEED = 10`、`ABSORB_ARRIVE_DIST = 1.2` 与 `MUST_TAGS = { "lj_flame" }`；**收集半径的 16 没找到** | 16 码这个数字对吗？定义在哪个文件？ |
| 8 | ⑦ 法宝与工具 | 魂幡 | 「会自动吸收原版的 **4 种**影怪」 | 组件里看到白名单 `terrorbeak` / `crawlinghorror` / `crawlingnightmare`（第 4 个没抓到）+ `ONEOF_TAGS = { shadowcreature, nightmarecreature, lj_can_be_refined }` | 第 4 种是哪个？（我的抓取漏了一行）另外「影骑士/影主教/影车不会被吸」与白名单一致 ✓ |

## 二、可选补充（代码里明确有、wiki 没写，看要不要加）

| # | 模块 | 条目 | 代码里的事实 | 建议 |
|---|---|---|---|---|
| 1 | ② 生物入魔 | 中度 / 深度入魔 | 半血护盾的特效是**红色力场**：`fx.AnimState:SetMultColour(1, 0.15, 0.15, 1)` + `fx.Light:SetColour(1, 0.15, 0.15)` | 要不要在「限定易伤」那句后面补一句「护盾是红色力场」？ |
| 2 | ⑦ 法宝与工具 | 月魄凝液瓶 | 它有**祈雨**功能：`UseMoonRain` 消耗 `RAIN_COST = 20` 点灵液（注释「祈雨消耗20%灵液」），触发后世界设为一直降水、**10 分钟后恢复**；`finiteuses:SetMaxUses(100)` | wiki 的月魄凝液瓶卡片**完全没写祈雨** —— 要不要补？（这是玩家能用的功能）|

## 三、已解决（留档，不必再问）

| 模块 | 原来写的 | 处理 |
|---|---|---|
| ① 境界 | 经验 = 目标**入魔前**的最大生命 × 5% | 作者指示「按代码写的来」→ 已改成「目标的最大生命 × 5%；按代码取的是当前最大生命，被入魔抬高的那部分也算在内」（commit `1d8ac54`） |
| ⑪ 地形 | 蝴蝶岛「三棵阴郁之棘」等具体数字 | 作者要求改用模糊说法 → 已改成「若干阴郁之棘」等 |
| ⑩ 墟火 | 异火主人「落地后只有主人能捡」/「收进容器成为新主人」 | 代码是「首次掉落无主、只有葫芦吸收才定主人」→ 已订正（commit `00983e3`） |
| ⑩ 墟火 | 葫芦吸收「每秒扣 1 灵力值」 | 代码只有每秒 2 点生命 + 点燃一次 → 已订正（commit `00983e3`） |
| ⑩ 墟火 | 风干的羽毛「右键阅读」 | 它是炼丹炉材料，纸条在尘火日志里读 → 已订正（commit `c231bb6` / `ec1fd28`） |
| ③ 丹药 | 清心丸卡片没写「可解噬魂蛇毒」 | 作者同意补上 → 已加「额外用途：可以解毒——服用后直接解除噬魂蛇毒」 |
| ③ 丹药 | 炼丹总则没写「强异火必定成功、且不改变丹劫判定」 | 作者同意补上 → 已加 |
| ③ 丹药 | 炼丹总则没写「异火本体炼丹不消耗」 | 作者同意补上 → 已加「只有分裂出来的火焰才是消耗品，会消耗 1 个」 |
